= link_to "I'm studying", new_event_path, class: "btn btn-primary"
= link_to "Events now", events_path, class: "btn btn-primary"
= link_to "User interests", users_path, class: "btn btn-primary"

%h5
  My tags:
  = raw current_user.tags.map(&:name).map { |t| link_to t, users_path(:tag => t), class: "bg-info" }.join(', ')
  
%h1 Feed