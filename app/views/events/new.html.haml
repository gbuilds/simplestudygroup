%h3 What are you working on?

= form_for(@event) do |f|
 = f.label :title
 </br>
 = f.text_field :title, size: "50", placeholder: "What's the title of your event?"
 </br>
 = f.label :description, "Description"
 </br>
 = f.text_area :description, size: "50x2", placeholder: "Who? What? Where? Add a quick a description for people to join you"
 </br>
 = f.label :tag_list, "Tags"
 </br>
 = f.text_field :tag_list, size: "50", placeholder: "Add tags (separated by commas)"
 </br>
 = f.label :start_time
 = f.datetime_select :start_time, ampm: true, default: Time.now
 </br>
 = f.label :end_time
 = f.datetime_select :end_time, ampm: true, default: Time.now, start_year: Time.now.year
 </br>
 = f.submit "Create it", class: "btn btn-sm btn-primary"